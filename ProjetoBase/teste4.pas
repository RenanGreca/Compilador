program escopoProc (input, output);
    var x, y: integer;
    c: boolean;
    procedure k(var t: integer);
        begin
        end;

    procedure p(a: integer; var b: integer);
        var z: integer;
        begin
		  write(x);
          k(y);
        end;
    begin
        p(1, x);
    end.
